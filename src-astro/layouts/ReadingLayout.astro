---
import "../../src/index.css";
import "../../src/styles/fonts.css";
import {
  buildThemeModeBootstrapScript,
  getThemeModeDocumentAttributes,
} from "../../src/lib/theme-mode-bootstrap";

interface Props {
  canonicalUrl?: string;
  description: string;
  image?: string;
  imageAlt?: string;
  robots?: string;
  title: string;
}

const {
  canonicalUrl: _canonicalUrlProp = "",
  description: _description,
  image: _image = "",
  imageAlt: _imageAlt = "",
  robots: _robots = "noindex, nofollow",
  title: _title,
} = Astro.props;
const locals = Astro.locals.nekomata;
const siteName = String(locals?.siteSettings?.site?.name || "Nekomata").trim() || "Nekomata";
const separator = locals?.siteSettings?.site?.titleSeparator || " | ";
const pageTitle = _title ? `${_title}${separator}${siteName}` : siteName;
const canonicalUrl =
  _canonicalUrlProp ||
  new URL(
    `${Astro.url.pathname}${Astro.url.search}`,
    locals?.primaryAppOrigin || Astro.url.origin || "https://nekomata.moe",
  ).toString();
const absoluteImage = _image ? new URL(_image, canonicalUrl).toString() : "";
const themeModeDocument = getThemeModeDocumentAttributes(locals?.siteSettings);
const themeModeBootstrapScript = buildThemeModeBootstrapScript(locals?.siteSettings);
---

<!doctype html>
<html
  lang="pt-BR"
  data-page-meta="true"
  data-page-owner="astro"
  data-global-theme-mode={themeModeDocument.mode}
  data-theme-accent={themeModeDocument.accent}
  data-theme-mode={themeModeDocument.mode}
  class={themeModeDocument.className}
  style={themeModeDocument.style}
>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta name="description" content={_description} />
    <meta name="robots" content={_robots} />
    <meta name="theme-color" content="hsl(var(--background))" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={_description} />
    <meta property="og:type" content="article" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:site_name" content={siteName} />
    <meta property="og:locale" content="pt_BR" />
    {absoluteImage ? <meta property="og:image" content={absoluteImage} /> : null}
    {_imageAlt ? <meta property="og:image:alt" content={_imageAlt} /> : null}
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={_description} />
    <meta name="twitter:card" content={absoluteImage ? "summary_large_image" : "summary"} />
    {absoluteImage ? <meta name="twitter:image" content={absoluteImage} /> : null}
    {_imageAlt ? <meta name="twitter:image:alt" content={_imageAlt} /> : null}
    <link rel="canonical" href={canonicalUrl} />
    <script is:inline set:html={themeModeBootstrapScript} />
  </head>
  <body class="min-h-screen bg-background text-foreground">
    <slot />
  </body>
</html>
